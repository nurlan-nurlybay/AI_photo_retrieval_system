# Flutter frontend placeholder
# flutter run -d linux --dart-define=BACKEND_BASE_URL=http://127.0.0.1:8080 run this to run the app in Linux mode


# Your Mac's local IP (something like 192.168.x.x or 10.0.0.x)
ipconfig getifaddr en0  # for WiFi
# or
ipconfig getifaddr en1  # for Ethernet