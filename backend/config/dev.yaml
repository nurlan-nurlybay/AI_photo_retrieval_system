version: "1.0.0"

http:
  host: "0.0.0.0"
  port: 8080
  readTimeout: 5s
  writeTimeout: 5s
  idleTimeout: 30s

postgres:
  host: "172.22.16.1"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "media"
  sslmode: "disable"
  maxOpenConns: 10
  maxIdleConns: 5
  connMaxLifetime: 1h
  connMaxIdleTime: 600s
  healthCheckPeriod: 300s

redis:
  addr: "localhost:6379"
  password: ""
  db: 0
  dialTimeout: 5s
  readTimeout: 5s
  writeTimeout: 5s
  poolSize: 10

seaweedfs:
  baseURL: "http://localhost:8888"

clip:
  host: "localhost"
  port: 8003
  timeout: 5s
  maxIdleConn: 5

faiss:
  host: "localhost"
  port: 8002
  protocol: "http"
  timeout: 5s
  
log:
  level: "info"
  format: "text"
  sourceFolder: "backend"
